{
  "name": "@spectra/utils",
  "version": "3.0.0",
  "description": "Shared utility functions for SpectraAI",
  "main": "index.ts",
  "types": "index.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@spectra/types": "*",
    "zod": "^3.22.0",
    "date-fns": "^3.6.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0"
  }
}
